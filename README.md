# Simple WebApp

## Structure
- **frontend/** — static HTML, CSS, JS  
- **backend/** — Flask app, routes, services, models  
- **data/** — persistence layer  
- **tests/** — unit tests  

## Quickstart
1. cd simple-webapp  
2. pip install -r requirements.txt  
3. ./run.sh  
